import { BartimeDirective } from './bartime.directive';

describe('BartimeDirective', () => {
  it('should create an instance', () => {
    const directive = new BartimeDirective();
    expect(directive).toBeTruthy();
  });
});
